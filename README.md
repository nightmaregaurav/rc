Add the following line in .bashrc or .zshrc file
``` bash
source ~/<path>/<to>/<cloned>/<folder>/activate
```
OR

``` bash
cd ~ && git clone https://github.com/nightmaregaurav/rc.git && echo "source ~/rc/activate" >> .bashrc
```

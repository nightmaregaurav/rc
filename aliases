# To use sudo on aliases
alias sudo='sudo '

# timeout alias for osx
alias timeout='gtimeout'

# awk alias for osx
alias awk='gawk'

# private zsh
alias privatezsh=zsh -f --login -c 'unset HISTFILE; exec zsh'

# private bash
alias privatebash=bash --noprofile --norc -c 'set +o history; exec bash'

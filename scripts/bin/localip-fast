#!/usr/bin/env bash

# Help message
if [[ "$1" == "--help" || "$1" == "-h" ]]; then
  echo "Usage: localip-fast"
  echo
  echo "Fetches the local IP address stored in ~/.ip.local if it exists"
  echo
  echo "Examples:"
  echo "  localip-fast"
  exit 0
fi

cat ~/.ip.local

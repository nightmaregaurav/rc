#!/usr/bin/env bash

# Help message
if [[ "$1" == "--help" || "$1" == "-h" ]]; then
  echo "Usage: now"
  echo
  echo "Show the current date and time in a human-readable format."
  echo
  echo "Examples:"
  echo "  now"
  exit 0
fi

date "+%Y-%m-%d %A %T %Z"

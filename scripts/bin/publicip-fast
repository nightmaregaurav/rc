#!/usr/bin/env bash

SCRIPT=$(basename "$0")

HELP_MESSAGE="Usage: $SCRIPT
Fetches the public IP address stored in ~/.ip.public if it exists

Examples:
    $SCRIPT"

# Help message
if [[ "$1" == "--help" || "$1" == "-h" ]]; then
  echo "$HELP_MESSAGE"
  exit 0
fi

cat ~/.ip.public
